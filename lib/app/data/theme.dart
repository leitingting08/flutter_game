import 'package:flutter/material.dart';

class ThemeColors {
  // static Color primaryColor = Color.fromARGB(255, 255, 255, 0);
  static const Color primaryColor = Colors.amberAccent;
  static const Color textColor = Colors.white70;
}
